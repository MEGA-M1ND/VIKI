"""Retrieval quality evaluation framework for VIKI Company Brain.

This package provides golden evaluation cases (:mod:`app.eval.golden`),
result models (:mod:`app.eval.metrics`), and a runner (:mod:`app.eval.runner`)
that measures precision@5, MRR, noise rate, and latency against hardcoded
ground-truth queries.
"""
